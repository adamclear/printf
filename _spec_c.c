#include "main.h"
#include <stdarg.h>
/**
 * _spec_c - returns a string with a character in it
 * @n: input character variable
 * Return: ptr_n
 */
char * _spec_c(void *n)
{
	char *ptr_n;

	ptr_n = n;
return (ptr_n);
}
