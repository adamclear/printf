#include "main.h"
/**
 * _strlen - returns length of given string not including the null byte
 * @s: given string to get length
 * Return: Length of s
 */

int _strlen(char *s)
{
	int len = 0;

	while (s[len] != '\0')
		{
			len++;
		}
	return (length);
}
